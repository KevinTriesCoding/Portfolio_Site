# Kevin Moye – Portfolio (React / Vite)

This is the React + Vite version of the portfolio, using the same layout and styling as the original template with Kevin Moye's content (hero, about, work, process, contact).

## Getting Started

1. Run `npm install`
2. Run `npm run dev`

Build for production: `npm run build`
