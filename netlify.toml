[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for all routes (in case you add client-side routing later)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
